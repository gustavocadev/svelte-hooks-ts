import { describe, it } from 'vitest';
// import { createCounter } from '$lib';
// import { subscribe } from 'svelte/internal';

describe('sum test', () => {
	it('createCounter tests', () => {
		// expect(createCounter(12)).toBe({
		// 	counter: { subscribe: subscribe },
		// 	increment:
		// 		(by = 1) =>
		// 		(n: number) =>
		// 			n + by,
		// 	decrement:
		// 		(by = 1) =>
		// 		(n: number) =>
		// 			n - by,
		// 	reset: () => void {}
		// });
	});
});
